import PageContent from "./PageContent";

export const metadata = {
  title: "Bank Accounts — Meteoric Admin",
  description: "Manage bank accounts for invoice payments.",
};

export default function BankAccountsPage() {
  return <PageContent />;
}
