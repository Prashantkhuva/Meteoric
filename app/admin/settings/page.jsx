import PageContent from "./PageContent";

export const metadata = {
  title: "Settings — Meteoric Admin",
  description: "Manage your admin profile and account settings.",
};

export default function SettingsPage() {
  return <PageContent />;
}
