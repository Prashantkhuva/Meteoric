import PageContent from "./PageContent";

export const metadata = {
  title: "Users — Meteoric Admin",
  description: "Manage admin panel users and their roles.",
};

export default function UsersPage() {
  return <PageContent />;
}
