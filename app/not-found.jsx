export const metadata = {
  title: "404 — Page Not Found | Meteoric",
  robots: "noindex, nofollow",
};

import NotFoundPage from "@/page-content/NotFound";

export default function NotFound() {
  return <NotFoundPage />;
}
